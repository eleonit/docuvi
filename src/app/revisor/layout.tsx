/**
 * Layout del Revisor
 */

import RevisorLayout from '@/components/layouts/RevisorLayout'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <RevisorLayout>{children}</RevisorLayout>
}
